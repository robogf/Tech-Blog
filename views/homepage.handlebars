{{#each posts as |post| }}
<div class="row align-center mb-5">
  <div class="col-md-5">
    <h2>{{post.title}}</h2>
  </div>
  <div class="col-md-5">
    <h3>{{project.body}}</h3>
  </div>
  <div class="col-md-2">
    <h3>{{project.user}}</h3>
  </div>
</div>
{{/each}}
